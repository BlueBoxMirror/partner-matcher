server:
  port: 8080
  servlet:
    context-path: /api
  address: 0.0.0.0

spring:
  # 数据库
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/partner_matcher?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 123456

  # QQ邮箱（587必须搭配 starttls，不要用 SSL）
  mail:
    host: smtp.qq.com
    port: 587
    username: 3068067142@qq.com
    password: tslrpexpkysedfbi   # 16位授权码，不是邮箱密码
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000

# MyBatis
mybatis:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
