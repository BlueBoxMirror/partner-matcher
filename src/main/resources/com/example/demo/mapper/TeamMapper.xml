<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.TeamMapper">
   <select id="selectPublicTeams" resultType="com.example.demo.pojo.Team">
        SELECT * FROM teams
        WHERE is_deleted = 0
        AND expire_time > NOW()
        AND (password IS NULL OR password = '')
        <if test="keyword != null and keyword != ''">
            AND team_name LIKE CONCAT('%', #{keyword}, '%')
        </if>
        ORDER BY created_at DESC
    </select>
   <select id="selectByCreator" resultType="com.example.demo.pojo.Team">
        SELECT * FROM teams
        WHERE create_user_id = #{creatorId} AND is_deleted = 0
        ORDER BY created_at DESC
    </select>
   <select id="selectByIdsWithPage" resultType="com.example.demo.pojo.Team">
        SELECT * FROM teams
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND is_deleted = 0
        ORDER BY created_at DESC
    </select>
</mapper>